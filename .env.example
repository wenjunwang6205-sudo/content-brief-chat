# Vercel Environment Variables (do not commit real values)

LLM_API_KEY=sk-your-key-here
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# Optional: requests per IP per minute (default 10)
RATE_LIMIT_PER_MIN=10

# Demo frontend (GitHub Pages) — set to your Vercel deployment URL
# VITE_API_BASE=https://your-project.vercel.app
