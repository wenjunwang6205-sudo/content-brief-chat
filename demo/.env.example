# API URL (no trailing slash). Leave empty when using `vercel dev` proxy.
# Production default is in .env.production (Tencent SCF).
VITE_API_BASE=https://1330417914-fcrpnf219x.ap-guangzhou.tencentscf.com
